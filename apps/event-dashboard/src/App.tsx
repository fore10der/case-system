import React from "react";

import { CaseSystemTheme, LeftMenuLayout } from "@case-system/components";

const App: React.FC = () => {
  return (
    <CaseSystemTheme>
      <LeftMenuLayout />
    </CaseSystemTheme>
  );
};

export default App;
